# Managed by the dotfiles installer.
# Signing-key trust remains unchanged until the repository fingerprint is
# independently verified and pinned by the installer.
[omarchy]
SigLevel = Optional TrustAll
Server = https://pkgs.omarchy.org/stable/$arch
