#!/usr/bin/env bash

menu=$(cat <<EOF
  Lock
  Restart
  Shutdown
EOF
)

choice=$(echo "$menu" | fuzzel --dmenu --width=30)

case "$choice" in
  *Lock)
    ~/.local/bin/coffee-lock-screen
    ;;
  *Restart)
    systemctl reboot
    ;;
  *Shutdown)
    systemctl poweroff
    ;;
esac
