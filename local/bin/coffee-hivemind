#!/bin/bash

query=$(echo "" | fuzzel --dmenu --placeholder=" Ask the hivemind…" --width=40 --lines=0 --line-height=30 --vertical-pad=8)
[[ -n "$query" ]] && coffee-launch-term-float coffee-hivemind-run "$query"
