#!/bin/bash

exec j4-dmenu-desktop \
  --dmenu="dot-launch-menu" \
  --wrapper="uwsm-app --" \
  --term-mode=custom \
  --term="xdg-terminal-exec --title={name} -- {cmdline@}" \
  --no-generic \
  --log-level=ERROR
